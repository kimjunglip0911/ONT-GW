---
alwaysApply: true
---
> **위치·최신화**: 본문은 레포 디렉터리 트리 정본이다. **구조가 바뀌면 같은 작업에서 이 파일을 반드시 갱신**한다.

```
ONT-GW/
├── .cursor/
│   └── rules/                       # Cursor 에이전트 규칙
│       ├── project-structure.mdc    # 레포 디렉터리 트리 정본
│       └── rules.mdc                # 스택·명명·Git 개발 규칙
├── docs/
│   └── plans/                       # 구현 플랜
├── app/                             # App Router 루트
│   ├── _auth/                       # DB 사용자 세션
│   │   ├── check.ts                 # 비밀번호·역할 매핑
│   │   ├── cookie.ts                # 역할·아이디 쿠키 설정·삭제
│   │   ├── fold.ts                  # 아이디 대문자 정규화
│   │   ├── fold.test.ts             # 아이디 정규화 테스트
│   │   ├── gate.ts                  # 공개 경로·next 허용
│   │   ├── gate.test.ts             # 경로 게이트 테스트
│   │   ├── kick.ts                  # 세션 무효 시 로그인으로 보냄
│   │   ├── live.ts                  # 쿠키와 DB 행이 맞는지 본다
│   │   ├── names.ts                 # 쿠키 키·역할 값
│   │   ├── role.ts                  # 관리자·로그인 여부
│   │   ├── role.test.ts             # 역할 매핑 테스트
│   │   ├── rule.ts                  # 새 비밀번호 검증
│   │   ├── rule.test.ts             # 새 비밀번호 검증 테스트
│   │   ├── seed.ts                  # 기본 비밀번호 상수
│   │   ├── seed.test.ts             # 기본 비밀번호 테스트
│   │   ├── sess.ts                  # 세션 유효 여부
│   │   └── sess.test.ts             # 삭제·초기화 세션 테스트
│   ├── _db/                         # Supabase 서버 접근
│   │   ├── add.ts                   # public.users insert
│   │   ├── asnote.ts                # DB 행 → Notice
│   │   ├── cast.ts                  # DB 행 → 목록 User
│   │   ├── cols.ts                  # 목록 select 컬럼
│   │   ├── del.ts                   # public.users 삭제
│   │   ├── find.ts                  # uid로 로그인 행 조회
│   │   ├── html.ts                  # 공지 본문 태그 정제
│   │   ├── html.test.ts             # 본문 정제 테스트
│   │   ├── hues.ts                  # 공지 본문 허용 글자 색
│   │   ├── list.ts                  # public.users 조회
│   │   ├── mark.ts                  # 공지 본문 허용 인라인 스타일
│   │   ├── note.ts                  # public.notices insert
│   │   ├── notes.ts                 # public.notices 조회
│   │   ├── pwd.ts                   # 비밀번호 수정·시드일 때만 변경
│   │   ├── rank.ts                  # 고정·최신순 정렬
│   │   ├── rank.test.ts             # 고정 정렬 테스트
│   │   ├── sb.ts                    # secret 키 클라이언트
│   │   ├── sizes.ts                 # 공지 본문 허용 글자 크기
│   │   └── upd.ts                   # public.users 수정
│   ├── _shell/                      # 레이아웃 셸
│   │   ├── auth.tsx                 # 로그인·로그아웃 버튼
│   │   ├── brand.tsx                # 레일 로고·앱 이름
│   │   ├── chip.ts                  # 메뉴 행 활성 클래스
│   │   ├── cx.ts                    # 클래스 문자열 합치기
│   │   ├── draw.tsx                 # 모바일 드로어 메뉴
│   │   ├── frame.tsx                # 레일·탑바·드로어 조립
│   │   ├── glyph.tsx                # 메뉴 아이콘 매핑
│   │   ├── hover.tsx                # 데스크톱 레일 펼침
│   │   ├── kids.tsx                 # 하위 메뉴 링크
│   │   ├── links.tsx                # 권한별 주 메뉴
│   │   ├── menu.ts                  # 직원·관리자 메뉴 정의
│   │   ├── nest.tsx                 # 접히는 관리자 하위 메뉴
│   │   ├── rail.tsx                 # 접힌 아이콘 레일
│   │   ├── sider.tsx                # 펼친 텍스트 사이드바
│   │   ├── title.ts                 # 경로별 페이지 제목
│   │   └── topbar.tsx               # 상단 제목·모바일 메뉴
│   ├── admin/                       # 관리자 화면
│   │   ├── attend/                  # 근태 관리 목업
│   │   │   ├── data.ts              # 직원 근태 목업 행
│   │   │   ├── page.tsx             # 근태 관리 페이지
│   │   │   └── table.tsx            # 근태 목록 테이블
│   │   ├── notice/                  # 공지사항 등록 (DB)
│   │   │   ├── edit.tsx             # 본문 Tiptap 편집기
│   │   │   ├── form.tsx             # 공지 작성 폼
│   │   │   ├── form.test.ts         # 본문 라벨이 도구를 감싸지 않음
│   │   │   ├── hues.tsx             # 본문 글자 색 팔레트
│   │   │   ├── meta.tsx             # 제목·구분·고정 일수
│   │   │   ├── page.tsx             # 공지사항 등록 페이지
│   │   │   ├── save.ts              # 공지 등록 액션
│   │   │   ├── sizes.tsx            # 본문 글자 크기
│   │   │   └── tools.tsx            # 본문 서식 도구
│   │   ├── pay/                     # 급여 세팅 목업
│   │   │   ├── form.tsx             # 시급·수당 비율 폼
│   │   │   └── page.tsx             # 급여 세팅 페이지
│   │   ├── users/                   # 사용자 등록 & 조회 (DB)
│   │   │   ├── acts.tsx             # 행 수정·삭제 버튼
│   │   │   ├── chg.ts               # 행 수정 저장 액션
│   │   │   ├── data.ts              # 사용자 타입
│   │   │   ├── drop.ts              # 행 삭제 액션
│   │   │   ├── ecell.tsx            # 행 수정 입력 칸
│   │   │   ├── erow.tsx             # 수정 중인 목록 행
│   │   │   ├── field.tsx            # 등록 폼 입력 칸
│   │   │   ├── filt.ts              # 이름·ID 조회 필터
│   │   │   ├── find.tsx             # 사용자 조회 입력
│   │   │   ├── form.tsx             # 단건 등록 폼
│   │   │   ├── head.ts              # 엑셀 필수·양식 헤더
│   │   │   ├── ids.ts               # ONT 자동 ID 생성
│   │   │   ├── inputs.tsx           # 등록 폼 신원 필드
│   │   │   ├── pays.tsx             # 등록 폼 급여 필드
│   │   │   ├── load.ts              # xlsx 시트 읽기
│   │   │   ├── map.ts               # 셀 값을 Draft로 변환
│   │   │   ├── map.test.ts          # Draft에 비밀번호가 필요 없음
│   │   │   ├── page.tsx             # 사용자 등록 & 조회 페이지
│   │   │   ├── panel.tsx            # 사용자 목록 테이블
│   │   │   ├── parse.ts             # 파일·시트 검증·파싱
│   │   │   ├── parse.test.ts        # 엑셀 헤더에 PW가 없어도 파싱
│   │   │   ├── patch.ts             # 행 수정 값 검증
│   │   │   ├── patch.test.ts        # 행 수정이 비밀번호를 안 넣음
│   │   │   ├── rbtn.tsx             # 목록 PW 초기화 버튼
│   │   │   ├── read.ts              # 폼·셀에서 Draft 읽기
│   │   │   ├── rst.ts               # 비밀번호 1234 초기화 액션
│   │   │   ├── rst.test.ts          # 초기화 전 관리자 가드
│   │   │   ├── save.ts              # 단건·다건 저장 액션
│   │   │   ├── shell.tsx            # 업로드·폼·목록 상태
│   │   │   ├── tmpl.ts              # 엑셀 양식 다운로드
│   │   │   ├── trow.tsx             # 목록 한 행
│   │   │   ├── upload.tsx           # 엑셀 올리기·양식 받기
│   │   │   └── use.ts               # 목록 상태·저장·삭제
│   │   ├── box.ts                   # 관리자 폼 공통 클래스
│   │   ├── hub.tsx                  # 하위 화면 바로가기 카드
│   │   └── page.tsx                 # 관리자 허브 페이지
│   ├── api/                         # Route Handlers
│   │   ├── kick/
│   │   │   └── route.ts             # 무효 세션 GET, 쿠키 삭제 후 로그인
│   │   ├── login/
│   │   │   └── route.ts             # 로그인 POST, 쿠키 또는 변경 필요
│   │   ├── logout/
│   │   │   └── route.ts             # 로그아웃 POST, 쿠키 삭제
│   │   └── passwd/
│   │       └── route.ts             # 기본 비밀번호 변경 POST, 쿠키 발급
│   ├── login/                       # 로그인 화면
│   │   ├── card.tsx                 # 아이디·비밀번호 카드
│   │   ├── chg.tsx                  # 강제 변경 팝업
│   │   ├── chg.test.ts              # 팝업에 취소·라벨 감싸기 없음
│   │   ├── fields.tsx               # 아이디·비밀번호 칸
│   │   ├── form.tsx                 # 로그인 폼
│   │   ├── npw.tsx                  # 새 비밀번호·확인 칸
│   │   └── page.tsx                 # /login 페이지
│   ├── attend/                      # 직원 근태 확인
│   │   ├── board.tsx                # 내 출퇴근 일별 표
│   │   ├── cards.tsx                # 오늘·주간 근태 요약
│   │   ├── data.ts                  # 내 근태 목업 데이터
│   │   └── page.tsx                 # 근태 확인 페이지
│   ├── notice/                      # 직원 공지
│   │   ├── data.ts                  # 공지 타입
│   │   ├── item.tsx                 # 목록 행·본문 펼침
│   │   ├── list.tsx                 # 공지 목록 UI
│   │   └── page.tsx                 # 공지 페이지
│   ├── globals.css                  # Tailwind·테마 토큰
│   ├── layout.tsx                   # 루트 레이아웃·살아 있는 세션 확인
│   └── page.tsx                     # /notice 로 리다이렉트
├── .gitignore                       # Git 제외 목록
├── AGENTS.md                        # Next.js 에이전트 안내
├── CLAUDE.md                        # AGENTS.md 위임
├── eslint.config.mjs                # ESLint 설정
├── next.config.ts                   # Next.js 설정
├── package.json                     # 스크립트·의존성
├── package-lock.json                # 잠긴 의존성 버전
├── postcss.config.mjs               # PostCSS(Tailwind)
├── proxy.ts                         # 쿠키 유무·직원 /admin 차단
├── README.md                        # 목적·실행·환경 변수
├── scripts/
│   └── run.cjs                      # 로컬 CA 있으면 Node에 연결
├── ts6hook.cjs                      # ESLint용 TS 6 래퍼
└── tsconfig.json                    # TypeScript 컴파일 설정
```

- `node_modules/`, `.next/` 는 생성물이며 트리에 넣지 않는다.
- `.env.local` 은 로컬 전용(Git 제외). 빈 템플릿이라 `public/` 은 아직 없다.

*Last Updated: 2026-08-14*
